# 학습 경로 (Learning Path)

이 문서는 벡터 데이터베이스 인덱싱 코스의 상세한 주차별 학습 로드맵을 제공합니다.

## 📋 전체 개요

| 주차 | 주제 | 난이도 | 핵심 개념 | 예상 시간 |
|-----|------|--------|---------|---------|
| 1 | Flat Index | ⭐ 초급 | 벡터 검색 기본, O(n) | 5-10시간 |
| 2-3 | IVF Index | ⭐⭐ 중급 | 클러스터링, nprobe | 10-15시간 |
| 4-6 | HNSW Index | ⭐⭐⭐ 고급 | 그래프, 계층 구조 | 15-20시간 |
| 7 | 종합 및 심화 | ⭐⭐⭐ | 최적화, 실전 활용 | 5-10시간 |

**총 예상 시간**: 35-55시간 (자신의 속도로 진행)

---

## Week 1: Flat Index - 기초 다지기

### 목표
- 벡터 유사도 검색의 기본 원리 이해
- 브루트포스 알고리즘 구현
- 테스트, 벤치마크, 동시성 처리 학습

### 학습 순서

#### Day 1: 환경 설정 및 이해 (2시간)

1. **프로젝트 구조 파악**
   ```bash
   # 전체 구조 확인
   tree -L 2

   # pkg/ 유틸리티 이해
   cd pkg/vector && cat vector.go
   cd ../distance && cat distance.go
   ```

2. **테스트 실행**
   ```bash
   # 유틸리티 테스트 (모두 통과해야 함)
   cd pkg/vector && go test -v
   cd ../distance && go test -v
   cd ../testdata && go test -v
   ```

3. **01-flat/README.md 정독**
   - 알고리즘 이해
   - 시간 복잡도 분석
   - 구현할 내용 파악

**체크포인트**:
- [ ] pkg/ 구조 이해함
- [ ] 거리 메트릭 (L2, Cosine, Dot) 차이 이해
- [ ] Flat index 알고리즘 설명 가능

#### Day 2-3: 기본 구현 (4-6시간)

1. **NewFlatIndex 구현**
   ```go
   // 1. Config 검증
   // 2. 필드 초기화
   // 3. 테스트: TestNewFlatIndex
   ```

2. **Add 구현**
   ```go
   // 1. 벡터 검증
   // 2. 차원 일치 확인
   // 3. 저장
   // 4. 테스트: TestBasicAdd, TestDimensionMismatch
   ```

3. **Size 구현**
   ```go
   // 간단하지만 thread-safe해야 함
   // 테스트: TestBasicAdd에서 확인
   ```

**실습 팁**:
```bash
# TDD 사이클
go test -v -run=TestBasicAdd        # Red
# 구현...
go test -v -run=TestBasicAdd        # Green
```

**체크포인트**:
- [ ] TestNewFlatIndex 통과
- [ ] TestBasicAdd 통과
- [ ] TestDimensionMismatch 통과

#### Day 4-5: Search 구현 (4-6시간)

1. **기본 Search 로직**
   ```go
   // 1. 검증
   // 2. 모든 벡터와 거리 계산
   // 3. 정렬
   // 4. Top k 반환
   ```

2. **에지 케이스 처리**
   - 빈 인덱스
   - k > size
   - 차원 불일치

3. **테스트 통과**
   ```bash
   go test -v -run=TestEmptyIndex
   go test -v -run=TestSearchKLargerThanSize
   go test -v -run=TestSearchResultsSorted
   ```

**함정 경험**:
- `TestSearchKLargerThanSize`: k=100인데 10개만 있을 때?
- `TestSearchResultsSorted`: 정렬 빠뜨리면 실패!

**체크포인트**:
- [ ] TestEmptyIndex 통과
- [ ] TestSearchKLargerThanSize 통과
- [ ] TestSearchResultsSorted 통과
- [ ] TestSearchAccuracy 통과

#### Day 6: 동시성 및 마무리 (2시간)

1. **Race Condition 테스트**
   ```bash
   go test -race
   # RWMutex 제대로 사용했는지 확인
   ```

2. **벤치마크 실행**
   ```bash
   go test -bench=.
   # O(n) 스케일링 확인
   ```

3. **모든 테스트 통과 확인**
   ```bash
   go test -v
   # 16/16 tests passed
   ```

**체크포인트**:
- [ ] TestConcurrency 통과
- [ ] Race detector 통과
- [ ] 벤치마크 결과 이해
- [ ] 모든 테스트 통과

#### Day 7: 복습 및 비교 (1-2시간)

1. **Solution 비교**
   ```bash
   cd ../solution
   cat flat.go
   cat EXPLANATION.md
   ```

2. **차이점 분석**
   - 자신의 구현 vs solution
   - 더 나은 점, 부족한 점

3. **벤치마크 결과 기록**
   ```
   내 구현:
   BenchmarkSearch/size=100    →  ___ µs
   BenchmarkSearch/size=1000   →  ___ µs
   BenchmarkSearch/size=10000  →  ___ µs
   ```
   *(IVF/HNSW와 비교할 baseline)*

**체크포인트**:
- [ ] Solution 이해
- [ ] k-smallest 알고리즘 (정렬 vs heap) 차이 이해
- [ ] 벤치마크 결과 기록

### Week 1 마무리 체크리스트

- [ ] 모든 테스트 통과 (go test -v)
- [ ] Race detector 통과 (go test -race)
- [ ] 벤치마크 실행 및 이해
- [ ] EXPLANATION.md 읽고 이해
- [ ] 벡터 검색 기본 개념 습득
- [ ] O(n) 복잡도 체감

**다음 주 준비**: 02-ivf/README.md 미리 읽기

---

## Week 2-3: IVF Index - 클러스터링으로 가속화

*상세 계획은 `/home/roach/.claude/plans/cozy-wandering-rivest.md` 참고*

### 목표
- k-means 클러스터링 알고리즘 구현
- IVF 인덱스로 검색 가속화
- **nprobe 파라미터의 중요성 체득**

### 핵심 학습 포인트

#### Week 2: k-means 및 기본 IVF (8-10시간)

1. **k-means 알고리즘 구현**
   - 초기 중심점 선택 (k-means++)
   - 반복: 할당 → 갱신
   - 수렴 조건

2. **IVF 인덱스 구조**
   - Train() - 클러스터 생성
   - Add() - 클러스터 할당
   - Search() - nprobe 개 클러스터 검색

**중요 테스트**:
```bash
# 이 테스트가 함정!
go test -v -run=TestIVFRecallWithSmallNprobe
```

**예상 결과**:
```
nprobe=1: Recall=0.32 ❌
→ "Recall too low! Try increasing nprobe"

nprobe=5: Recall=0.75
nprobe=10: Recall=0.92 ✅
```

**체크포인트**:
- [ ] k-means 구현 완료
- [ ] Train, Add, Search 구현
- [ ] nprobe=1일 때 recall 저하 경험
- [ ] nprobe 조정으로 recall 향상 확인

#### Week 3: 파라미터 튜닝 및 최적화 (5-8시간)

1. **파라미터 실험**
   ```go
   for nprobe := 1; nprobe <= 20; nprobe++ {
       recall := measureRecall(idx, nprobe)
       latency := measureLatency(idx, nprobe)
       fmt.Printf("nprobe=%d: recall=%.2f, latency=%v\n",
           nprobe, recall, latency)
   }
   ```

2. **성능 비교**
   ```bash
   # Flat vs IVF 벤치마크
   cd 01-flat/solution && go test -bench=.
   cd ../../02-ivf/solution && go test -bench=.
   ```

3. **트레이드오프 이해**
   - nprobe ↑ → recall ↑, latency ↑
   - nprobe ↓ → recall ↓, latency ↓
   - 최적값 찾기: recall > 90%, latency 최소

**체크포인트**:
- [ ] 파라미터 튜닝 실험
- [ ] Recall vs latency 그래프 (머릿속으로)
- [ ] Flat보다 빠른 것 확인
- [ ] Solution과 비교

### Week 2-3 마무리 체크리스트

- [ ] k-means 알고리즘 이해 및 구현
- [ ] IVF 모든 테스트 통과
- [ ] nprobe 파라미터 중요성 체득
- [ ] Flat 대비 성능 향상 확인 (5-10배)
- [ ] 클러스터링 개념 확실히 이해

**다음 단계 준비**: 03-hnsw/README.md 읽기, 그래프 개념 복습

---

## Week 4-6: HNSW Index - 최신 기술 마스터

*상세 계획은 `/home/roach/.claude/plans/cozy-wandering-rivest.md` 참고*

### 목표
- 계층적 그래프 구조 이해
- HNSW 알고리즘 구현
- 복잡한 파라미터 (M, efConstruction, efSearch) 튜닝

### 핵심 학습 포인트

#### Week 4: 구조 설계 (6-8시간)

1. **기본 구조**
   - Node: 벡터 + 다층 연결
   - Layer: 계층 관리
   - Random level generation

2. **삽입 알고리즘 이해**
   - Top-down 탐색
   - Layer별 연결
   - Neighbor pruning

**체크포인트**:
- [ ] Node, Layer 구조 구현
- [ ] RandomLevel 이해
- [ ] TestHNSWBasic 통과

#### Week 5: 핵심 알고리즘 (8-10시간)

1. **Add 구현**
   - searchLayer 구현
   - selectNeighbors 구현
   - 연결 및 pruning

2. **Search 구현**
   - Layer-by-layer 탐색
   - efSearch 활용
   - Top k 반환

**함정 경험**:
```bash
# efSearch < k 함정
go test -v -run=TestHNSWEfSearchTooSmall
```

**예상 결과**:
```
efSearch=5, k=10
→ Error: "efSearch must be >= k"
```

**체크포인트**:
- [ ] Add 알고리즘 구현
- [ ] Search 알고리즘 구현
- [ ] efSearch 파라미터 이해
- [ ] 주요 테스트 통과

#### Week 6: 파라미터 튜닝 (4-6시간)

1. **M 파라미터 실험**
   ```
   M=4:  recall=0.70, slow build
   M=16: recall=0.95, good balance ✅
   M=32: recall=0.98, high memory
   ```

2. **efConstruction 실험**
   ```
   ef=40:  fast build, recall=0.85
   ef=200: slow build, recall=0.96 ✅
   ef=400: very slow, recall=0.98
   ```

3. **efSearch 실험**
   ```
   ef=10:  fast, recall=0.80
   ef=50:  balanced, recall=0.95 ✅
   ef=200: slow, recall=0.99
   ```

**체크포인트**:
- [ ] M 파라미터 이해
- [ ] efConstruction 파라미터 이해
- [ ] efSearch 파라미터 이해
- [ ] 모든 테스트 통과
- [ ] Solution과 비교

### Week 4-6 마무리 체크리스트

- [ ] HNSW 알고리즘 완전 이해
- [ ] 모든 테스트 통과
- [ ] 파라미터 튜닝 경험
- [ ] Flat/IVF 대비 성능 비교
- [ ] Sub-linear 검색 시간 확인

---

## Week 7: 종합 및 심화

### 목표
- 세 인덱스 종합 비교
- 실전 활용 사례 학습
- 최적화 및 개선

### 학습 활동

#### Day 1-2: 비교 분석 (4-6시간)

1. **examples/01-basic-search 실행**
   ```bash
   cd examples/01-basic-search
   go run main.go
   ```

2. **성능 표 작성**
   | Index | Build | Search (k=10) | Recall | Memory |
   |-------|-------|--------------|--------|---------|
   | Flat  | 즉시  | ___ ms       | 100%   | ___ MB  |
   | IVF   | ___ s | ___ ms       | ___% | ___ MB  |
   | HNSW  | ___ s | ___ ms       | ___% | ___ MB  |

3. **언제 어떤 인덱스를 쓸까?**
   - Flat: n < 10K, 100% 정확도 필요
   - IVF: 10K < n < 1M, 균형
   - HNSW: n > 100K, 최고 성능

**체크포인트**:
- [ ] 세 인덱스 성능 비교 완료
- [ ] 각 인덱스의 use case 이해

#### Day 3-4: 이론 심화 (3-4시간)

1. **docs/THEORY.md 읽기**
   - Curse of dimensionality
   - ANN vs kNN
   - 이론적 배경

2. **docs/COMMON_PITFALLS.md 읽기**
   - 디버깅 팁
   - 흔한 실수
   - 해결 방법

3. **docs/FURTHER_READING.md**
   - 논문 읽기
   - 다른 알고리즘 조사

**체크포인트**:
- [ ] 이론적 배경 이해
- [ ] Product Quantization 등 추가 기법 조사
- [ ] 실제 논문 1-2개 읽기

#### Day 5-7: 프로젝트 및 최적화 (5-8시간)

**선택 1: 최적화 도전**
- Flat index에 heap 적용
- IVF에 병렬 검색 추가
- HNSW neighbor selection 개선

**선택 2: 실전 프로젝트**
- 텍스트 임베딩 검색 (OpenAI embeddings)
- 이미지 검색 (CLIP embeddings)
- 추천 시스템 구현

**선택 3: 추가 알고리즘**
- Annoy 구현
- LSH (Locality Sensitive Hashing)
- Product Quantization

**체크포인트**:
- [ ] 하나 이상의 심화 활동 완료
- [ ] 배운 내용 정리 (블로그 등)

### Week 7 마무리 체크리스트

- [ ] 세 인덱스 완전 이해
- [ ] 파라미터 튜닝 마스터
- [ ] 실전 활용 사례 이해
- [ ] 추가 학습 자료 섭렵
- [ ] 포트폴리오 작성 (선택)

---

## 🎓 최종 체크리스트

### 기술 역량
- [ ] 벡터 유사도 검색 원리 설명 가능
- [ ] 브루트포스, 클러스터링, 그래프 알고리즘 구현 완료
- [ ] ANN 개념 및 트레이드오프 이해
- [ ] Go 동시성 프로그래밍 숙련
- [ ] 테스트 및 벤치마킹 능력 향상

### 실전 적용
- [ ] 언제 어떤 인덱스를 쓸지 판단 가능
- [ ] 파라미터 튜닝 능력
- [ ] 실제 벡터 DB (Milvus, Weaviate) 문서 이해 가능
- [ ] 프로덕션 레벨 고려사항 인지

---

## 📝 학습 기록 템플릿

각 주차 완료 후 작성하세요:

### Week X 회고

**배운 것**:
-

**어려웠던 점**:
-

**해결 방법**:
-

**다음 주 목표**:
-

**벤치마크 결과**:
```
(복사 붙여넣기)
```

---

## 🚀 다음 단계

과정 완료 후:

1. **GitHub에 올리기**
   - 자신의 구현 공유
   - README 작성

2. **블로그 작성**
   - 배운 내용 정리
   - 함정 경험 공유

3. **실전 프로젝트**
   - 벡터 검색 서비스 구축
   - 추천 시스템 구현

4. **심화 학습**
   - Faiss 소스코드 분석
   - 최신 논문 읽기
   - 다른 알고리즘 구현

---

**Good luck on your learning journey! 🎉**
